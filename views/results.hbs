<div class="header">
      <img src="/img/google.png" alt="Google Logo" />
      <form class="resform">
        <input class="searchbar" type="text" />
        <button>Search</button>
      </form>
    </div>
    <div class="content">
      <div class="numres">
        <p>{{results.length}} Results</p>
      </div>
      {{#each results as |e|}}
      <div class="result">
        <p class="smalltext">{{e.url}}</p>
        <h2><a href="{{e.url}}">{{e.title}}</a></h2>
        <p> {{e.description}}
        </p>
        <ul>
          {{#each e.links as |l|}}
          <li><a href="{{l.url}}">{{l.title}}</a></li>
          {{/each}}
        </ul>
      </div>
      {{/each}}
    </div>